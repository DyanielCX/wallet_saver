# [VULN-11] CWE-16 Misconfigured services for IaC scanning.
version: "3"
services:
  app:
    build: .
    privileged: true                       # privileged container
    ports:
      - "0.0.0.0:8080:8080"
    environment:
      DB_PASSWORD: Pg_demo_password_2026    # hardcoded secret
    volumes:
      - /:/host                            # mounts host root
  db:
    image: postgres:9.6                     # outdated image
    environment:
      POSTGRES_PASSWORD: postgres           # weak/default password
